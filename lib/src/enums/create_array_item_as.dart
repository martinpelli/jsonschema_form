/// Different options for creating array items
enum ArrayItemAs {
  /// Create a new array item inside the view itself
  inner,

  /// Create a new array item by opening a new dialog
  dialog,

  /// Create a new array item by opening a new screen
  screen;
}
